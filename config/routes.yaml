home:
    path: /{_locale}
    controller: App\Controller\SecurityController::index
    requirements:
        _locale: '%app_locales%'
    defaults:
        template: default/homepage.html.twig
        _locale: '%locale%'

