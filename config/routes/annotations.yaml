controllers:
    resource: ../../src/Controller/
    type: annotation
    prefix: /{_locale}
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

api:
    resource: ../../src/Controller/Api/
    prefix: /api
    type: annotation
    name_prefix: api_
