<form method="post" action="{{ path('amp_delete', {'id': amp.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amp.id) }}">
    <button class="btn form-delete-button">Eliminar</button>
</form>
