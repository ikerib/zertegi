{% extends 'base.html.twig' %}

{% block title %}Ciriza{% endblock %}
{% block header %}Ciriza{% endblock %}
{% block header_desc %}{{ ciriza.id }} ikusten{% endblock %}
{% block breadcrumbs %}
    <li class="active">Ciriza</li>
    <li class="active">Ikusi</li>
{% endblock %}

{% block body %}
    <div class="row">

        <div class="col-xs-12">

            <div class="box">
                <div class="box-header">
                    <div class="box-tools box-tools-left" data-toggle="tooltip" title="" data-original-title="Status">
                        <a class="btn btn-sm btn-primary" href="{{ path('ciriza_edit', {'id': ciriza.id}) }}"><i class="fas fa-edit"></i> Aldatu</a>
                        <a class="btn btn-sm btn-default" href="{{ path('ciriza_index') }}"><i class="fa fa-fw fa-arrow-alt-circle-left" aria-hidden="true"></i> {{ 'Zerrendara itzuli' | trans }}</a>
                        <div class="box-tools pull-right">
                            <a id="btnPrint" class="btn btn-sm btn-default" href="{{ path('ciriza_print', {'id': ciriza.id}) }}"><i class="fa fa-fw fa-print"></i> {{ "Inprimitu" | trans }}</a>
                        </div>
                    </div>
                </div>
                <div class="box-body">
                    <div class="row">&nbsp;</div>
                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Signatura</th>
                            <td>{{ ciriza.signatura  | raw }}</td>
                        </tr>
                        <tr>
                            <th>Data</th>
                            <td>{{ ciriza.data  | raw }}</td>
                        </tr>
                        <tr>
                            <th>Deskribapena</th>
                            <td>{{ ciriza.deskribapena  | raw }}</td>
                        </tr>
                        <tr>
                            <th>Sailkapena</th>
                            <td>{{ ciriza.sailkapena }}</td>
                        </tr>
                        <tr>
                            <th>Oharrak</th>
                            <td>{{ ciriza.oharrak | raw  }}</td>
                        </tr>
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>

{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('jsDeleteForm') }}
{% endblock %}
