{{ form_start(form) }}
<div class="col-xs-6">
    <div class="text-center"><h3>Zertegi</h3></div>
    <div class="row">&nbsp;</div>
    {{ form_row(form.enpresa) }}
    {{ form_row(form.gaia) }}
    {{ form_row(form.helbidea) }}
    {{ form_row(form.izena) }}
    {{ form_row(form.kontsulta) }}
</div>

<div class="col-xs-6">
    <div class="text-center"><h3>Knosys</h3></div>
    <div class="row">&nbsp;</div>
    {{ form_row(form.berrikusiEnpresa) }}
    {{ form_row(form.berrikusiGaia) }}
    {{ form_row(form.berrikusiHelbidea) }}
    {{ form_row(form.berrikusiIzena) }}
    {{ form_row(form.berrikusiKontsulta) }}
</div>


<div style="display: none;">
    <button id="hiddenbutton" class="btn">{{ button_label|default('Gorde eta zerrendatik kendu') }}</button>
</div>

{{ form_end(form) }}
